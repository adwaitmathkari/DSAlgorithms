/**
 * @param {string} s
 * @param {string} t
 * @return {string}
 */
 var minWindow = function(s, t) {
    let p1 = 0;
    let p2 = 0;
    let ls = s.length;
    while (p2 < ls || p1 < ls) {
        
        
    }
};